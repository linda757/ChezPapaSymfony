{% extends 'base.html.twig' %}

{% block title %}Page d'inscription{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="offset-md-3 col-md-6">
                <div class="jumbotron py-3 px-3">

                    <h1 class="text-center">Inscrivez-vous</h1>
                    {{ form_start(form) }}

                    {{ form_widget(form) }}
                        <button type="submit" class="btn btn-primary">Confirmez votre inscritpion</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>


{% endblock %}